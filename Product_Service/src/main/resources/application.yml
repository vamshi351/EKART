spring:
  application:
    name: product-service

  datasource:
    url: jdbc:mysql://localhost:3306/vamshi
    username: root
    password: vaanara
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
    properties:
      hibernate:
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: krishnasait0951@gmail.com
    password: "kzap cwhq azmt ywcj"   # wrap in quotes to avoid parsing errors
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8081

jwt:
  secret: 849dc646881d1f09f1efba30100badb63f12ac4b899694a4f0dd01dd7998638d
  expiration:
    ms: 86400000

springdoc:
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    com.example.userservice: DEBUG
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG

# Eureka client config
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    fetch-registry: true
    register-with-eureka: true
  instance:
    prefer-ip-address: true
    
    
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
    
